version: 0.0
os: linux

files:
  - source: /
    destination: /var/lib/jenkins/workspace/deployment\ job/springboot-backend

permissions:
  - object: /
    pattern: "**"
    owner: root
    group: root

hooks:
  ApplicationStop:
    - location: scripts/stop.sh
      timeout: 20
      runas: root
  ApplicationStart:
    - location: scripts/start.sh
      timeout: 20
      runas: root
  ValidateService:
    - location: scripts/validate.sh
      timeout: 120
      runas: root
